package game;

/**
 * This enum specifies all the special tiles that are placed
 * over an area's background image to trigger events.
 */
public enum Tile {
	EMPTY;

	/**
	 * This is the width and height of a single tile in pixels.
	 */
	public static final int TILE_SIZE = 20;
}
